#配置编码
spring:
  http:
    encoding:
      charset: utf-8
      enabled: true
      force: true
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/theOlds?serverTimezone=Asia/Shanghai&useUnicode=true&useSSL=false&autoReconnect=true&characterEncoding=UTF-8&allowMultiQueries=true&rewriteBatchedStatements=true
    username: root
    password: root
#配置端口
server:
  port: 9090
  servlet:
    context-path: /theOldsWeb
logging:
  level:
    com.theoldsweb.myweb.web.mapper: debug